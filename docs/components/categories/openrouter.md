---
title: openrouter Components
description: Audited Flowi Workflow built-in components in the openrouter category.
---

# openrouter components

Machine-readable reference: [`../../ai/components/openrouter.json`](../../ai/components/openrouter.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `openrouter.OpenRouterComponent` | OpenRouter | AVAILABLE | OpenRouter provides unified access to multiple AI models from different providers through a single API. |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
