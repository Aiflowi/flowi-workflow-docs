---
title: unstructured Components
description: Audited Flowi Workflow built-in components in the unstructured category.
---

# unstructured components

Machine-readable reference: [`../../ai/components/unstructured.json`](../../ai/components/unstructured.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `unstructured.Unstructured` | Unstructured API | AVAILABLE | Uses Unstructured.io API to extract clean text from raw source documents. Supports a wide range of file types. |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
