---
title: embeddings Components
description: Audited Flowi Workflow built-in components in the embeddings category.
---

# embeddings components

Machine-readable reference: [`../../ai/components/embeddings.json`](../../ai/components/embeddings.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `embeddings.EmbeddingSimilarityComponent` | Embedding Similarity | LEGACY | Compute selected form of similarity between two embedding vectors. |
| `embeddings.TextEmbedderComponent` | Text Embedder | LEGACY | Generate embeddings for a given message using the specified embedding model. |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
