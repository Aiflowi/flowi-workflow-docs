---
title: Schedule Trigger in Flowi Workflow
description: Use the built-in Flowi Schedule Trigger to start workflows on a schedule.
---

# Schedule Trigger

The Schedule Trigger is a built-in Flowi trigger component for scheduled workflow execution.

Use it when the workflow should start based on time rather than an incoming message or HTTP request.

For exact current fields and outputs, read:

[`../ai/components/input_output.json`](../ai/components/input_output.json)
