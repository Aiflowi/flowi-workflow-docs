---
title: wolframalpha Components
description: Audited Flowi Workflow built-in components in the wolframalpha category.
---

# wolframalpha components

Machine-readable reference: [`../../ai/components/wolframalpha.json`](../../ai/components/wolframalpha.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `wolframalpha.WolframAlphaAPI` | WolframAlpha API | AVAILABLE | Enables queries to WolframAlpha for computational data, facts, and calculations across various topics, delivering structured responses. |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
