---
title: redis Components
description: Audited Flowi Workflow built-in components in the redis category.
---

# redis components

Machine-readable reference: [`../../ai/components/redis.json`](../../ai/components/redis.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `redis.Redis` | Redis | AVAILABLE | Implementation of Vector Store using Redis |
| `redis.RedisChatMemory` | Redis Chat Memory | AVAILABLE | Retrieves and store chat messages from Redis. |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
