---
title: prototypes Components
description: Audited Flowi Workflow built-in components in the prototypes category.
---

# prototypes components

Machine-readable reference: [`../../ai/components/prototypes.json`](../../ai/components/prototypes.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `prototypes.PythonFunction` | Python Function | LEGACY | Define and execute a Python function that returns a Data object or a Message. |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
