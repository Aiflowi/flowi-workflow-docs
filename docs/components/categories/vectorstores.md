---
title: vectorstores Components
description: Audited Flowi Workflow built-in components in the vectorstores category.
---

# vectorstores components

Machine-readable reference: [`../../ai/components/vectorstores.json`](../../ai/components/vectorstores.json)

| Component ID | Display name | Status | Description |
|---|---|---|---|
| `vectorstores.LocalDB` | Local DB | LEGACY | Local Vector Store with search capabilities |

For new workflows, prefer `AVAILABLE` components. Do not create new flows with `LEGACY` components unless maintaining older workflows.
